{
  "name": "backend",
  "version": "1.0.0",
  "description": "Jewelry E-commerce Backend API",
  "main": "dist/server.js",
  "author": "",
  "license": "ISC",
  "scripts": {
    "dev": "cross-env NODE_ENV=development ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsc --build tsconfig.json",
    "start": "cross-env NODE_ENV=production tsc --build tsconfig.json && node dist/server.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.58.0",
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "fastify": "^5.6.1",
    "mongoose": "^8.18.3",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "cross-env": "^10.1.0",
    "pino-pretty": "^13.1.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.2"
  }
}
